pipeline {
    agent any

    stages {
        stage ('checkout') {
            steps {
                checkout scm
            }
        }

        stage ('Build the code') {
            steps {
                sh ''
            }
        }
    }
}